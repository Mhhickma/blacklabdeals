name: Add Shared Site Search
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  shared-search-already-applied:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm shared search files are already committed
        run: |
          echo "Shared search was applied directly to site-header.js and search.html."
          echo "This workflow is kept only as a manual placeholder so GitHub Actions no longer reports invalid YAML."

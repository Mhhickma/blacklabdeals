name: DISABLED - Add Meta Pixel

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This old one-time workflow has been disabled and no longer changes the site."
